<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="bibliotheque.View.AddBookPage"
             Title="Ajouter un livre">

    <VerticalStackLayout>
        <Entry Placeholder="Titre" Text="{Binding Title}" />
        <Entry Placeholder="Autheur" Text="{Binding Author}" />
        <Entry Placeholder="Maison d'édition" Text="{Binding PublishingHouse}" />
        <Editor Placeholder="Description" HeightRequest="250" Text="{Binding Description}"  />
        <Button Margin="20" Text="Envoyer" Command="{Binding Path=AddBookCommand}" />
        <Button Margin="20" Text="Retour" Command="{Binding Path=BackCommand}" />
       
        
    </VerticalStackLayout>
</ContentPage>
